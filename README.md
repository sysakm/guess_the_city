# Guess the city

A simple React application where players guess a city from its photo and four answer options.

The app includes a lightweight username-based login used to select a local player profile. It does not provide authentication. Each player's statistics and the last selected username are stored separately in localStorage.

Educational project created as part of a FullStack programming course.
